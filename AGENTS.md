# stock-mobile 强制规则（违反 = 事故）

本文件是项目铁律。任何 agent（Hermes / Codex / 其他）操作本项目前必须完整阅读并遵守。
用户对"精简版/功能缺失/版本回退"零容忍。2026-08-18 已发生一次事故：未对比就部署重建版，导致线上功能缺失 172KB，用户暴怒。

## 铁律 1：禁止任何形式的功能精简
- 恢复/重建/重构后的代码，未经用户明确确认，不得部署。
- 部署前必须与线上当前完整版做**逐功能对比验证**，输出对比清单（功能项 × 存在/缺失），确认无缺失才可部署。
- 用户原话："恢复/重建前端绝不允许功能等价精简版——必须完整还原原版，未经同意不得砍功能。"
- 任何人不得以"时间紧/文件大/反推难"为由跳过对比。对比方法：
  - 构建产物体积对比（缺 100KB+ = 必有问题）
  - 功能关键词/文案字符串对比（grep 关键 UI 文案）
  - 无头浏览器（Playwright/Puppeteer）实测每个 tab 渲染 + 关键交互
- 发现缺失 → 停止部署，补齐后再上，并向用户如实汇报差异。

## 铁律 2：部署前先备份可回退
- 部署前四重备份：git 分支 + 未提交 patch + 文件副本 + 数据库副本。
- 线上旧文件（assets 等）**永远保留，增量添加，禁止 rm -rf**。
- 回滚方案必须先想好再动手：旧 index.html/sw.js/JS 文件确认在线，回滚 = 改引用，不删文件。

## 铁律 3：部署后必须实测验证
- "build 通过"不等于"可以上线"。部署后必须无头浏览器实测：页面渲染、无 console 错误、关键数据非 0。
- 验证版本三重确认：页面 ver-tag / 服务器 JS 哈希 / SW 版本，三者一致才算部署成功。
- 用户报障时优先信用户，先复现排查，禁止用"缓存/刷新试试"搪塞。

## 铁律 4：服务器资源红线
- 服务器只有 2 核 / 2GB 内存。全市场打分等重任务**禁止**在服务器上直接跑（会 swap 风暴卡死整个服务器，SSH 都进不去）。
- 重任务要么分批小并发、要么降内存占用、要么先本地算好再同步结果。跑之前先估算内存。
- 任何后台长任务启动后，必须能通过 SSH/API 随时中止（记录 PID/任务 ID）。

## 铁律 5：改动必汇报
- 每次代码改动必须主动汇报：改了什么文件、改了什么内容、为什么改、效果是什么。说人话。
- 方案未确认前禁止执行（用户说"我让你执行了吗"= 立即停）。

## 铁律 6：数据一致性
- 投资数据禁止代理/近似/当日值替代历史数据。
- 前端展示与筛选口径必须一致。
- 同一数据不同视图要一致，数据没变结果必须一样。
